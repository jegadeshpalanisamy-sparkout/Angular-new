import { FilterCustomPipePipe } from './filter-custom-pipe.pipe';

describe('FilterCustomPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCustomPipePipe();
    expect(pipe).toBeTruthy();
  });
});
